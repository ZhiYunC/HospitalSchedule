@{ Layout = null; } @*取消導航欄*@
@using Demo.Models;
@{
    Doctor doctor=ViewBag.doctor;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{pageTitle}}</title>
    <!-- 其他共享的头部信息 -->
    <style>
        @* body {
            font-family: Arial, sans-serif;
            margin-top: 0;
            padding: 0;
            background-color:white ;
        } *@
        @* .fixed{
            position: fixed;
            z-index: 1000;
        } *@

        header {
            display: flex;
            @* justify-content: space-between; *@
            align-items: center;
            color: rgb(0, 0, 0);
            padding-left: 15px;
            padding-right: 30px;
            @* height: 70px; /* 調整高度 */ *@
            @* background-color: rgba(242, 242, 242, 0.82); *@
            @* background-color:#D6EFFF/*亮藍*/; *@
            @* background-color:#D9DFE7;/*淺藍*/ *@
            @* background-color:#4a5759;/*深綠*/ *@
            @* background-color:#847577; /*深咖*/*@
            @* background-color: #CCE4F0; *@
            background-color: white;
            vertical-align: bottom;
            
        } 
        /*讓物件在水平方向上置中*/
        .center{
            display: flex;
            justify-content: center; /* 水平置中 */
        }
        

        .remind{
            font-size: 20px;
            background-color: #e6cfcf;
            border-radius: 5px ; /* 添加圓角 */
            padding: 5px;
        }
        hr {
            border: none;
            height: 1px;
            background-color: #e6e4e4;
            box-shadow: 0 4px 8px rgba(5, 5, 5, 0.3);
            margin: 0;
            
        }
        /*導航欄樣式*/
        nav {
            display: flex;
            justify-content: flex-start;
            @* margin-right: 80px; *@
            margin-left: 550px;
            @* margin-top: 15px; *@
            justify-content: space-between;
            font-size: 18px;
            @* font-weight: bold; *@
        }

        nav a {
            @* color:white; *@
            color: #6e6e6e;
            text-decoration: none;
            padding: 15px;
            margin-top: 15px;
            position: relative;
            transition: color 0.3s;
            @* border: 1px solid;
            border-color:#4A79D1; *@
        }
        /*底線效果*/
         nav a:after {
            content: '';
            display: block;
            width: 0;
            height: 3px;
            background-color: #4A79D1;
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            /*transition: width 0.3s;*/
            border-bottom: 2px solid transparent; /* 初始時設定底線為透明 */
            /*border-radius: 5px 5px 0 0;  添加圓角 */
        } 
        nav a:hover {
            color: #4A79D1;
        }

        nav a:hover:after {
            width: 100%;
        }
 
        /*有下拉選單的按鈕樣式*/
        .settings-button {
            background-color: transparent;
            border: none;
            @* color:white; *@
            color: #6e6e6e;
            cursor: pointer;
            padding: 15px;
            margin-top: 10px;
            text-decoration: none;
            position: relative;
            transition: color 0.3s;
            font-size: 18px;
            @* font-weight: bold; *@
            justify-content: flex-start;
        }
        .settings-button:hover {
            color: #4A79D1;
        }
        /*下拉選單*/
        .dropdown {
            position: relative;
            display: inline-block;
        }

        .dropdown-content {
            display: none;
            position: absolute;
            background-color: #f9f9f9;
            min-width: 125px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            z-index: 1;
            text-align: center;
            transform: translateX(-10%);
        }
    
        .dropdown-content p {
            color: #858484;
            padding: 12px;
            text-decoration: none;
            display: block;
            font-size: 16px;
        }

        .dropdown-content a {
            color: #333;
            padding: 10px;
            text-decoration: none;
            display: block;
        }

        .dropdown-content a:hover {
            background-color: #4A79D1;
            color: white;
        }

        .dropdown:hover .dropdown-content {
            display: block;
        }
        
        .dropdown img{
            width: 30px;
            height: 30px;
            
        }
        
        @* <醫護人員排班系統> *@
        .title{
            font-size: 22px;
            margin-left:-4px ;
            color:#5A5859 ;
            @* color:#4A79D1; *@
            font-weight: bold;
            margin-top: 25px;
            font-family: SimSun;
            }

        img{
            height: 65px;
            
        }
        .icon {
            position: fixed;
            width: 300px; /* 設置圖片寬度 */
            height: auto; /* 讓高度根據寬度等比例縮放 */
            bottom: 45px; /* 與底部的距離，可調整 */
            right: 11px; /* 與右側的距離，可調整 */
        }
        .icon:hover {
            background-color: #4A79D1;
            color: white;
        }
        .fixed{
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            background-color: #333;
            z-index: 1000; /* 設定一個足夠大的 z-index 值 */
        }
    </style>
</head>
<body>
    <div class="fixed">
        <header>
                <a href="/Manager/Calender" >
                    <img src="~/image/fjcu_logo.png" >
                </a>
                <div class="title">醫護人員排班系統</div>
                <nav>
                    @* <div class="dropdown">
                        <button class="settings-button">查看班表</button>
                        <div class="dropdown-content">
                            <a href="/Manager/Index">暫定班表</a>
                            <a href="/Manager/calender">歷史班表</a>
                        </div>
                    </div> *@
                    <a href="/Doctor/Index">暫定班表</a>
                    <a href="/Doctor/HistorySchedule">歷史班表</a>
                    <a href="/Doctor/Scheduling" >排班</a>
                    @* <a href="/Doctor/Test" >展開班表測試</a> *@
                    @* <a href="/Manager/doctor_date" >醫生偏好日期</a> *@
                    <div class="dropdown">
                        <button class="settings-button">
                            <img src="~/image/setting.png" alt="more">
                            </a>
                        </button>
                        <div class="dropdown-content">
                            <div class="dropdown-content-people">
                                <p>@doctor.Doctor_Name </p><div style="font-size:20px;">醫生</div>
                                <p>科別：@doctor.Doctor_Department</p>
                            </div>
                            <a href="/Home/Index">登出</a>
                        </div> 
                    </div>
                </nav>
                @* <div class="icon">
                    <a href="/Manager/read">
                        <img src="~/image/phone3.png" alt="more">
                    </a>
                </div>      *@
                
        </header>

        <hr>
    </div>
    <!-- 其他共享的内容 -->

    <!-- 共享的页脚信息 -->
</body>
</html>
